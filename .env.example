# --- BACKEND (Runtime) ---
# Configurações de E-mail (SMTP)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=seu-email@gmail.com
EMAIL_PASS=sua-senha-de-app
RECEIVER_EMAIL=email-que-recebe-leads@gmail.com

# Database (se aplicável, no momento o Neon é mencionado no package.json)
DATABASE_URL=postgres://user:password@host/db?sslmode=require

# --- FRONTEND (Build-time) ---
# Caso precise de variáveis no frontend (Vite), use o prefixo VITE_
# Exemplo: VITE_API_URL=https://api.seusite.com
